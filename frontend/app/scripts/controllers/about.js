'use strict';

/**
 * @ngdoc function
 * @name one41feApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the one41feApp
 */
angular.module('one41feApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
